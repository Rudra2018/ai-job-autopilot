from dotenv import load_dotenv
load_dotenv()
import os

def schedule_interview(title, email, time_str):
    # Replace this with Google Calendar API logic
    print(f"📅 Scheduling '{title}' with {email} at {time_str}...")
    return True
